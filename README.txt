DELEGATE, MOTHERFUCKER — static site

Deploy the contents of this folder as-is. No build step is required.

Files:
- index.html — complete site
- delegate-agents-bg.png — generated background artwork used by the hero

Netlify: drag the unzipped folder into Netlify Drop.
Vercel: create a static project and deploy this folder.
GitHub Pages: commit both files at the repository root and enable Pages.

Important: keep index.html and delegate-agents-bg.png in the same directory.
